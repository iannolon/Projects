from random import randint
N = 10
W = 8

D = (W/2)
total = 0

for game in range (0,1000):
    r1 = randint(0,N+1)
    r2 = randint(0,N+1)
    if r2 <= r1:
        total += 1 #D
    else:
        total -= 1 #D
print(total)
